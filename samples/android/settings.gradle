rootProject.name = 'aws-v4-signer-android-sample'
include ':app'

includeBuild('../../') {
    dependencySubstitution {
        substitute module('com.novoda.aws.v4.signer:library-jvm') with project(':library')
    }
}
